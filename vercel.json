{
  "version": 2,
  "buildCommand": "PRISMA_MIGRATE_TIMEOUT=30000 prisma generate && PRISMA_MIGRATE_TIMEOUT=30000 prisma migrate deploy && next build",
  "installCommand": "npm install",
  "framework": "nextjs"
} 